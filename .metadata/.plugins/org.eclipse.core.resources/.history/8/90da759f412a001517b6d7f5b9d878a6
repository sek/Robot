import java.awt.Color;
import java.awt.Font;
import java.awt.Graphics;

public class PenShapeButton extends DrawButton {
	
	private boolean isSquare;
	
	Font font2;
	
	DrawGrid dGrid;
	
	public PenShapeButton(int x, int y, int w, int h, DrawGrid dg)
	{
		xPos = x;
		yPos = y;
		width = w;
		height = h;
		dGrid = dg;
		
		isSquare = true;
		
		font2 = new Font("Arial", Font.BOLD, 12);
	}
	
	public void draw(Graphics g)
	{
		g.setColor(new Color(220, 220, 255));
		g.fillRect(xPos, yPos, width, height);
		g.setColor(Color.BLACK);
		g.drawRect(xPos, yPos, width, height);
		g.drawRect(xPos + 1, yPos + 1, width - 2, height - 2);
		g.drawRect(xPos + 2, yPos + 2, width - 4, height - 4);
		
		if(isSquare)
		{
			g.fillRect(xPos + 5, yPos + 5, 40, 40);
		}
		else
		{
			g.fillOval(xPos + 5, yPos + 5, 40, 40);
		}
		
		g.setFont(font2);
		g.drawString("PEN SHAPE", xPos, yPos);
		
	}
	
	@Override
	public void leftMouseClicked(int x, int y) {
		isSquare = !isSquare;
	}

}
